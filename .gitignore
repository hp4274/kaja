# db-config.php is tracked on purpose (the app needs it to reach the database).
# db-config.example.php is the blank template.

# Queued leads, stored documents, and logs (contain sensitive client/PHI data)
/storage/queue/
/storage/documents/
/storage/logs/

# Local scratch and test artifacts
/_backup_pre_formfix/
/docs/
/.reticle/
/.playwright-mcp/
*.log
*.yml
*.yaml

# All Markdown documentation files (workspace rule)
*.md
**/*.md
